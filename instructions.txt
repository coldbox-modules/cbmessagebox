********************************************************************************
INSTRUCTIONS
********************************************************************************
Just drop into your modules folder or use the box-cli to install

box install messagebox

The module registers the MessageBox model: messagebox@messagebox

Then you can use it as normal.  You can modify the module settings for style overrides
and template overrides.