/**
 * My Event Handler Hint
 */
component {

	// Index
	any function index( event, rc, prc ){
		cbMessageBox().light( "Light defeats darkness!" );
	}

	// Run on first init
	any function onAppInit( event, rc, prc ){
	}

}
